/* Leia uma data (dia, mês e ano), verifique e escreva se a data é ou não válida. */
const prompt =require('prompt-sync')()
function main(){
    const dia = Number(prompt('Qual dia deseja verificar a existência'))
    const mes = Number(prompt('Qual dia deseja verificar a existência'))
    const ano = Number(prompt('Qual dia deseja verificar a existência'))
}if(mes <= 12){
    
}