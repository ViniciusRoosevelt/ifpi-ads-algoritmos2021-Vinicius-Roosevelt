/* Leia uma letra, verifique se letra é "F" ou "M" e escreva F - Feminino, M - Masculino, Sexo Inválido. */
const prompt = require('prompt-sync')()
function main(){
    var genderM=form.gender_male.value;
    var genderF=form.gender_female.value;

    if(genderM.checked==false && genderF.checked==false )
       {
            alert("Selecione M ou F,respectivamente para Masculino ou Feminino");
            return false;
       }   
 }main()