// Entrada - Leia um valor em m, calcule e escreva o equivalente em cm.
const prompt = require('prompt-sync')()
const m = Number(prompt('Quantos metros deseja transformar em centímetros: '))
// Processamento - 1 m = 100 cm
const cm = m * 100
// Saída

